/** Flat full-bleed settings sections on mobile; cards on desktop. */
export const eventWorkspaceSettingsSurfaceClassName =
  "rounded-none bg-background py-0 shadow-none ring-0 md:rounded-xl md:bg-card md:py-(--card-spacing) md:ring-1 md:ring-foreground/10";

/** iOS-style grouped settings on mobile; spaced cards on desktop. */
export const eventWorkspaceSettingsStackClassName =
  "-mx-4 flex flex-col gap-5 bg-muted/30 py-4 pb-6 md:mx-0 md:gap-4 md:bg-transparent md:py-0 md:pb-0";
