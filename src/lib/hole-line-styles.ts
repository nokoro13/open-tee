/** Screen-space hole line width in CSS pixels — never scales with map zoom. */
export const HOLE_LINE_WIDTH_PX = 2;

/** Wider invisible hit area for tap-to-add-dogleg (screen pixels). */
export const HOLE_LINE_HIT_WIDTH_PX = 14;
